[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "-m 'not integration'"

[tool.pytest.ini_options.markers]
integration = "Integration tests that require running external services (n8n, Docker)"
