# --- Core Network & Environment ---
httpx==0.28.1
python-dotenv==1.2.2
litellm>=1.56.0
mem0ai>=0.1.0
tenacity>=8.2.0

# --- Image Processing & Vision ---
Pillow==12.2.0
pytesseract==0.3.13

# --- System & GUI Automation ---
psutil==7.2.2

# --- AI & Search Tools ---
ddgs==9.12.1
yfinance==1.2.0

# --- Document Parsing (Advanced) ---
pypdf==5.4.0
html2text==2025.4.15
openpyxl==3.1.5
pandas==2.2.3

# --- Browser Automation (Playwright — headless Chromium) ---
# After pip install, run: playwright install chromium
playwright==1.51.0

# --- Voice & Audio ---
gTTS==2.5.4
SpeechRecognition==3.15.2

# --- REST API ---
fastapi==0.135.3
uvicorn[standard]==0.42.0
python-multipart==0.0.20

# --- Setup Utilities ---
setuptools==69.0.3
pyyaml==6.0.3
watchdog==6.0.0
numpy==1.26.4
docker==7.1.0

# --- Database (PostgreSQL + pgvector) ---
psycopg[binary]==3.3.3
psycopg_pool==3.3.0

# --- Semantic Memory (TF-IDF for session memory retrieval) ---

# --- Observability (OpenTelemetry + Jaeger) ---
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-exporter-otlp==1.40.0
opentelemetry-instrumentation-fastapi==0.61b0

# --- Testing ---
pytest-asyncio>=0.23.0

# --- Dev / Linting ---
mypy>=1.10.0
ruff>=0.4.0
